# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /documentation                      controllers.Application.documentation(version = "1.4", page = "index" )
GET     /documentation/                     controllers.Application.documentation(version = "1.4", page = "index" )
GET     /documentation/:version             controllers.Application.documentation(version, page = "index" )
GET     /documentation/:version/            controllers.Application.documentation(version, page = "index" )
GET     /documentation/:version/:page       controllers.Application.documentation(version, page)
GET     /documentation/:version/:page/      controllers.Application.documentation(version, page)
GET     /                                   controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                       controllers.Assets.at(path="/public", file)
